<UserControl
    x:Class="SemanticsAr.App.Views.PlaceholderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:SemanticsAr.App.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PlaceholderViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Margin="32" VerticalAlignment="Center" HorizontalAlignment="Left">
        <TextBlock FontSize="28" FontWeight="SemiBold" Text="{Binding Title}" />
        <TextBlock
            Margin="0,12,0,0"
            MaxWidth="520"
            FontSize="15"
            Opacity="0.85"
            TextWrapping="Wrap"
            Text="{Binding Message}" />
    </StackPanel>
</UserControl>
